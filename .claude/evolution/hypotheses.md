# 假设追踪器

> 提出假设 → 设计验证 → 记录结果 → 提取教训
> 未被验证的假设 = 未被消化的经验

---

## 活跃假设

<!-- 格式：日期  假设内容  验证方法  状态 -->

| 日期 | 假设 | 验证方法 | 结果 | 教训 |
|------|------|---------|:--:|------|
| | | | ⏳ | |

---

## 已验证

<!-- 从上方移下来，附结论 -->

---

## 反事实回顾

> 每次修 Bug 后必填：如果 X 能更早发现这个 Bug，X 是什么？
> 然后实现 X。

| 日期 | Bug | 什么能更早发现 | 实施了没 |
|------|-----|--------------|:---:|
| 05-26 | Gateway 3 次崩溃（Scheduled Task cmd 包装+IdleSettings 杀进程） | 一个 `/gateway` 健康检查 + 每次会话启动自动验证 | ✅ `/gateway` 命令 + gateway-boot.vbs |
| 05-26 | Zod v4 `errors`→`issues` 导致 197 个测试返回 500 | 依赖升级后自动跑全量测试 | ✅ hooks: 改 tests/ → 自动跑 jest |
| 05-26 | logger.info is not a function（工厂函数缺静态方法） | mock 和实现接口一致性检查 | ⚠ 部分：middleware hook 只检查 Zod，未检查 logger 接口 |
